/** Canonical production site URL, used for metadataBase, canonical URLs,
 * sitemap.xml, robots.txt, Open Graph URLs, and JSON-LD. Override via
 * NEXT_PUBLIC_APP_URL for local/staging environments. */
export const SITE_URL = process.env.NEXT_PUBLIC_APP_URL ?? "https://pennypilot.pro";
